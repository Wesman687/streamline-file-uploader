# Stream-Line File Server Environment Configuration
# =======================================================
# 
# This file contains all environment variables used by the file server.
# Copy this file to .env and modify values as needed for your deployment.

# =======================================================
# 🚀 REQUIRED FOR STARTUP (with defaults provided)
# =======================================================

# Service Token for API Authentication
# This token is used by your applications to authenticate with the file server
AUTH_SERVICE_TOKEN=ee6d52ece4fa6c4c8836820d2eb7feeb6c78cbf2e2661ef76c9f5a805fc16340

# File Upload Signing Key
# Used for generating secure upload URLs (change in production)
UPLOAD_SIGNING_KEY=default-signing-key-change-in-production-for-security

# =======================================================
# 📁 STORAGE CONFIGURATION
# =======================================================

# Root directory for file storage
UPLOAD_ROOT=/app

# Maximum file size per upload (in MB)
MAX_BODY_MB=5120

# Per-user storage quota (in GB)
PER_USER_QUOTA_GB=500

# =======================================================
# 📋 LOGGING CONFIGURATION
# =======================================================

# Directory for log files
LOG_DIR=/app/services/upload/logs

# =======================================================
# 🔐 JWT AUTHENTICATION (OPTIONAL)
# =======================================================
# These are optional - if not provided, only service token auth will work
# JWT authentication allows user-based access control

# JWT Algorithm (RS256, HS256, etc.)
AUTH_JWT_ALG=RS256

# JWT Issuer (who issued the token)
# AUTH_JWT_ISSUER=your-auth-service

# JWT Audience (who the token is intended for)
# AUTH_JWT_AUDIENCE=your-file-server

# JWT Key ID (for JWKS)
# JWKS_KID=your-key-id

# JWT Public Key (Base64 encoded)
# AUTH_JWT_PUBLIC_KEY_BASE64=your-base64-encoded-public-key

# OR JWT Public Key File Path
# JWT_PUBLIC_KEY_PATH=/path/to/public-key.pem

# =======================================================
# 🌐 SERVER CONFIGURATION
# =======================================================

# Server port
PORT=8000

# Python path for imports (Docker)
PYTHONPATH=/app/services/upload

# =======================================================
# 🛡️ SECURITY NOTES
# =======================================================
# 
# For production deployments:
# 1. Change AUTH_SERVICE_TOKEN to a unique, secure token
# 2. Change UPLOAD_SIGNING_KEY to a unique, secure key
# 3. Set up proper JWT authentication if using user tokens
# 4. Adjust file size and quota limits as needed
# 5. Use environment-specific values for different deployments
#
# For development:
# The default values provided will work out of the box
#
# =======================================================
